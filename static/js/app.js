const root=document.getElementById('app');
const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=value=>`₹${Number(value||0).toLocaleString('en-IN',{maximumFractionDigits:0})}`;
const api=async(path,options={})=>{const response=await fetch(`/api${path}`,{headers:{'Content-Type':'application/json'},...options});const data=await response.json();if(!response.ok)throw Error(data.error||'Request failed');return data};
const toast=message=>{const node=document.createElement('div');node.className='toast';node.textContent=message;document.body.append(node);setTimeout(()=>node.remove(),3000)};
const navItems=[['dashboard','Overview'],['requests','Requests'],['new','New request'],['approvals','Approvals'],['vendors','Vendors'],['orders','Orders'],['analytics','Analytics']];
function shell(user,view){return `<div class="workspace"><aside class="sidebar"><div class="brand">PROCURE<span>AI</span></div><nav class="nav">${navItems.map(([id,label])=>`<button class="${view===id?'active':''}" onclick="go('${id}')">${label}</button>`).join('')}</nav><div class="user-card"><strong>${esc(user.name)}</strong>${esc(user.role)}<br><button class="button secondary" style="margin-top:14px;padding:8px 11px" onclick="logout()">Sign out</button></div></aside><main class="main" id="main"></main>${assistantDock()}</div>`}
function assistantDock(){return `<button class="assistant-fab" onclick="toggleAssistant()" aria-label="Open PROCUREAI Assistant"><span>✦</span></button><aside class="assistant-panel" id="assistant-panel"><div class="assistant-head"><div><span class="eyebrow">Intelligence layer</span><strong>PROCUREAI Assistant</strong></div><button onclick="toggleAssistant()" aria-label="Close assistant">×</button></div><div class="assistant-messages" id="assistant-messages"><div class="assistant-message assistant"><strong>PROCUREAI Assistant</strong><p>Ask about requests, vendors, approvals, orders, or spend.</p></div></div><form class="assistant-form" onsubmit="sendAssistant(event)"><input id="assistant-input" placeholder="Ask procurement anything..." autocomplete="off"><button class="button" aria-label="Send message">↗</button></form></aside>`}
function toggleAssistant(){document.body.classList.toggle('assistant-open');if(document.body.classList.contains('assistant-open'))document.getElementById('assistant-input')?.focus()}
async function sendAssistant(event){event.preventDefault();const input=document.getElementById('assistant-input');const message=input.value.trim();if(!message)return;const messages=document.getElementById('assistant-messages');messages.insertAdjacentHTML('beforeend',`<div class="assistant-message user"><p>${esc(message)}</p></div>`);input.value='';try{const result=await api('/ai/chat',{method:'POST',body:JSON.stringify({message})});messages.insertAdjacentHTML('beforeend',`<div class="assistant-message assistant"><strong>PROCUREAI Assistant</strong><p>${esc(result.message)}</p>${(result.actions||[]).map(action=>`<button class="assistant-action" onclick="location.href='${esc(action.route)}'">${esc(action.label)} ↗</button>`).join('')}</div>`);messages.scrollTop=messages.scrollHeight}catch(error){messages.insertAdjacentHTML('beforeend',`<div class="assistant-message assistant"><p>${esc(error.message)}</p></div>`)}}
function landing(authenticated){return `<div class="landing"><header class="landing-nav"><a class="landing-brand" href="/">Procure<span>AI</span></a><nav class="landing-links"><a href="#product">Product</a><a href="#solutions">Solutions</a><a href="#workflow">How it works</a><a href="#intelligence">Intelligence</a><a href="#pricing">Pricing</a></nav><div class="landing-actions"><a class="text-link" href="${authenticated?'/dashboard':'/login'}">${authenticated?'Open workspace':'Sign in'}</a><a class="button" href="${authenticated?'/dashboard':'/login'}">Get started</a></div><button class="menu-button" onclick="document.body.classList.toggle('menu-open')">Menu</button></header><main><section class="landing-hero"><div class="hero-copy"><span class="eyebrow">AI-powered procurement intelligence</span><h1>Stop managing procurement.<br><em>Start optimizing it.</em></h1><p>ProcureAI turns vendor data, risk signals, and approval workflows into one clear decision layer for every purchase.</p><div class="actions"><a class="button" href="${authenticated?'/dashboard':'/login'}">Get started <span>↗</span></a><a class="watch-link" href="#workflow"><span class="play">▶</span> See how it works</a></div><div class="hero-proof"><span>BUILT FOR MODERN PROCUREMENT TEAMS</span><b>Vendor intelligence</b><b>Risk-aware approvals</b><b>ERP-ready</b></div></div><div class="hero-visual"><div class="orbit orbit-one"></div><div class="orbit orbit-two"></div><div class="preview-card"><div class="preview-top"><span>LIVE DECISION ROOM</span><span class="live-dot">● LIVE</span></div><div class="preview-title">Purchase request <strong>PR-1028</strong></div><div class="preview-product">Industrial bearings <small>500 units · ₹4.8L budget</small></div><div class="analysis-line"><span class="line-dot done"></span><span>Requirements understood</span><b>100%</b></div><div class="analysis-line"><span class="line-dot done"></span><span>12 vendors compared</span><b>98%</b></div><div class="analysis-line"><span class="line-dot active"></span><span>Risk evaluated</span><b>LIVE</b></div><div class="recommend"><div><small>RECOMMENDED VENDOR</small><strong>ABC Industries</strong></div><div class="recommend-score">94<small>/100</small></div></div></div><div class="floating-note note-one">↓ 12% below market</div><div class="floating-note note-two">LOW RISK <span>●</span></div></div></section><section class="capability-strip" id="product"><span>ONE INTELLIGENCE LAYER FOR EVERY DECISION</span><div><b>01</b> Vendor intelligence</div><div><b>02</b> Risk analysis</div><div><b>03</b> Workflow automation</div><div><b>04</b> Procurement copilot</div></section><section class="split-section" id="solutions"><div><span class="eyebrow">From friction to flow</span><h2>Procurement should compound value, not admin.</h2></div><div class="problem-flow"><div class="flow-muted"><span>TRADITIONAL</span><p>Manual research <i>→</i> Spreadsheet comparison <i>→</i> Email approvals <i>→</i> Delayed decisions</p></div><div class="flow-bright"><span>WITH PROCUREAI</span><p>Intelligent matching <i>→</i> Risk detection <i>→</i> Smart routing <i>→</i> Better outcomes</p></div></div></section><section class="feature-section"><div class="section-intro"><span class="eyebrow">The intelligence layer</span><h2>Every signal. One sharper decision.</h2><p>Purpose-built intelligence for teams that need procurement to move quickly and hold up under scrutiny.</p></div><div class="feature-grid">${[['◈','AI procurement intelligence','Understand requirements and surface the context behind every request.'],['⌁','Vendor intelligence','Track reliability, delivery, accuracy, and commercial fit over time.'],['✦','Smart recommendations','Rank options using the factors your business actually cares about.'],['△','Procurement risk engine','See price, delivery, and supplier risks before they become surprises.'],['↗','Workflow automation','Route approvals and turn confident decisions into purchase orders.'],['?','Procurement copilot','Ask better questions about spend, vendors, and procurement activity.']].map(item=>`<article class="feature-card"><span class="feature-icon">${item[0]}</span><h3>${item[1]}</h3><p>${item[2]}</p><span class="feature-arrow">↗</span></article>`).join('')}</div></section><section class="workflow-section" id="workflow"><div class="section-intro"><span class="eyebrow">How ProcureAI works</span><h2>From request to result,<br>with fewer blind spots.</h2></div><div class="workflow-track">${[['01','Create request'],['02','Understand requirements'],['03','Compare vendors'],['04','Evaluate risk'],['05','Recommend'],['06','Approve & generate PO'],['07','Learn from outcome']].map((step,index)=>`<div class="workflow-step reveal"><span>${step[0]}</span><strong>${step[1]}</strong>${index<6?'<i>→</i>':''}</div>`).join('')}</div></section><section class="showcase-section" id="intelligence"><div class="showcase-copy"><span class="eyebrow">Vendor intelligence</span><h2>Know which suppliers are getting better.</h2><p>ProcureAI transforms historical performance into a living view of supplier health, so every new quote gets smarter context.</p><a class="text-link" href="${authenticated?'/dashboard#vendors':'/login'}">Explore the workspace ↗</a></div><div class="health-card"><div class="health-heading"><span>VENDOR HEALTH SCORE</span><b>↗ Improving</b></div><div class="health-score">92<small>/100</small></div><div class="health-bars">${[['Delivery performance',94],['Price competitiveness',87],['Order accuracy',96],['Reliability',91]].map(item=>`<div><span>${item[0]}</span><b>${item[1]}%</b><i><em style="width:${item[1]}%"></em></i></div>`).join('')}</div><div class="health-footer"><span>ABC Industries</span><span class="badge done">LOW RISK</span></div></div></section><section class="cta-section" id="pricing"><span class="eyebrow">Make the next decision better</span><h2>Turn procurement data<br>into better decisions.</h2><p>Bring intelligence, automation, and visibility to every purchasing decision.</p><a class="button" href="${authenticated?'/dashboard':'/login'}">Start building smarter procurement ↗</a></section></main><footer class="landing-footer"><b>Procure<span>AI</span></b><span>AI-powered procurement intelligence</span><span>© ProcureAI</span></footer></div>`}
async function login(){root.innerHTML=`<div class="login-wrap"><section class="login-art"><div class="login-mark">ProcureAI / operations</div><div><h1>Buy with<br>clarity.</h1><p>A calmer command center for requests, vendors, risk, and approvals.</p></div><div class="muted" style="color:#bbd2cb">Demo access: any listed account / password</div></section><section class="login-panel"><form class="login-box" id="login-form"><span class="eyebrow">Welcome back</span><h2>Sign in</h2><p>Enter your workspace credentials to continue.</p><div class="field"><label>Email address</label><input name="email" type="email" value="admin@demo.com" required></div><div class="field"><label>Password</label><input name="password" type="password" value="password" required></div><button class="button" style="width:100%;margin-top:12px">Enter workspace</button><div id="login-error" class="notice" style="display:none;margin-top:18px"></div></form></section></div>`;document.getElementById('login-form').onsubmit=async event=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.target));try{await api('/auth/login',{method:'POST',body:JSON.stringify(data)});location.href='/dashboard'}catch(error){const node=document.getElementById('login-error');node.textContent=error.message;node.style.display='block'}}}
async function dashboard(){const data=await api('/dashboard');return `<div class="topbar"><div><span class="eyebrow">Operations overview</span><h1>Good morning.</h1><p>One clear view of what needs attention across procurement.</p></div><button class="button" onclick="go('new')">+ New request</button></div><div class="stats">${[['Requests',data.stats.total_prs],['Needs review',data.stats.pending_approvals],['Purchase orders',data.stats.po_created],['Spend',money(data.stats.total_spend)]].map(item=>`<div class="stat"><span class="stat small">${item[0]}</span><strong>${item[1]}</strong></div>`).join('')}</div><div class="grid"><section class="panel"><div class="section-head"><h3>Recent requests</h3><a class="link" onclick="go('requests')">View all</a></div>${requestTable(data.recent_requests)}</section><section class="panel"><div class="section-head"><h3>Top vendors</h3><a class="link" onclick="go('vendors')">Performance</a></div>${data.top_vendors.map(v=>`<div style="display:flex;justify-content:space-between;border-bottom:1px solid var(--line);padding:14px 0;font:14px Arial,sans-serif"><span>${esc(v.name)}<small class="muted" style="display:block">${esc(v.category||'Supplier')}</small></span><b style="color:var(--teal)">${v.overall_score}/100</b></div>`).join('')}</section></div>`}
function requestTable(items){if(!items.length)return '<div class="empty">No requests yet.</div>';return `<table class="table"><thead><tr><th>Request</th><th>Product</th><th>Budget</th><th>Status</th></tr></thead><tbody>${items.map(pr=>`<tr><td><a class="link" onclick="go('detail/${encodeURIComponent(pr.id)}')">${esc(pr.id)}</a></td><td>${esc(pr.product)}<small class="muted" style="display:block">Qty ${pr.quantity}</small></td><td>${money(pr.budget)}</td><td><span class="badge ${pr.status==='PO_CREATED'?'done':pr.status==='REJECTED'?'no':'warn'}">${esc(pr.status.replaceAll('_',' '))}</span></td></tr>`).join('')}</tbody></table>`}
async function requests(){const items=await api('/purchase-requests');return `<div class="topbar"><div><span class="eyebrow">Procurement register</span><h1>Requests</h1><p>Track every request from analysis to purchase order.</p></div><button class="button" onclick="go('new')">+ New request</button></div><section class="panel">${requestTable(items)}</section>`}
function form(){return `<div class="topbar"><div><span class="eyebrow">Start a workflow</span><h1>New request</h1><p>Give the team enough context to make a confident buying decision.</p></div></div><section class="panel"><form id="request-form"><div class="form-grid"><div class="field"><label>Product *</label><input name="product" required placeholder="e.g. Laptop fleet"></div><div class="field"><label>Category</label><select name="category"><option>IT Hardware</option><option>Software</option><option>Office Supplies</option><option>Services</option><option>Other</option></select></div><div class="field"><label>Quantity *</label><input name="quantity" type="number" min="1" required></div><div class="field"><label>Budget per unit (₹) *</label><input name="budget" type="number" min="1" step=".01" required></div><div class="field"><label>Required delivery date *</label><input name="delivery_date" type="date" required></div><div class="field"><label>Priority</label><select name="priority"><option>Medium</option><option>Low</option><option>High</option><option>Critical</option></select></div><div class="field full"><label>Context</label><textarea name="description" rows="4" placeholder="What is this for? Any constraints?"></textarea></div></div><div class="actions" style="justify-content:flex-end;margin-top:16px"><button type="button" class="button secondary" onclick="go('requests')">Cancel</button><button class="button">Submit for analysis</button></div></form></section>`}
async function detail(id){const pr=await api(`/purchase-requests/${id}`);let compare=null;try{compare=await api(`/purchase-requests/${id}/comparison`)}catch{}return `<div class="topbar"><div><span class="eyebrow">Request ${esc(pr.id)}</span><h1>${esc(pr.product)}</h1><p>${esc(pr.category)} · Qty ${pr.quantity} · ${money(pr.budget)} per unit</p></div><button class="button secondary" onclick="go('requests')">Back to requests</button></div><div class="hero-card"><span class="badge">${esc(pr.status.replaceAll('_',' '))}</span><h2>${pr.status==='PO_CREATED'?'Purchase order created.':pr.status==='REJECTED'?'Request declined.':'Ready for a buying decision.'}</h2><p>${esc(pr.description||'No additional context provided.')}</p>${pr.status==='ANALYZING'?`<button class="button" onclick="analyze('${esc(pr.id)}')">Run vendor analysis</button>`:pr.status==='PENDING_APPROVAL'?`<button class="button" onclick="go('compare/${encodeURIComponent(pr.id)}')">Review vendor recommendation</button>`:pr.po_id?`<p style="color:var(--yellow);margin:0">PO reference: ${esc(pr.po_id)}</p>`:''}</div>${compare?`<section class="panel"><div class="section-head"><h3>Recommended supplier</h3><span class="score">${compare.recommended?.final_score||0}</span></div><p class="muted">${esc(compare.explanation)}</p><button class="button" onclick="go('compare/${encodeURIComponent(pr.id)}')">Open comparison</button></section>`:''}`}
async function compare(id){const data=await api(`/purchase-requests/${id}/comparison`);const rec=data.recommended;return `<div class="topbar"><div><span class="eyebrow">Decision room</span><h1>Compare vendors</h1><p>${esc(data.request.id)} · ${esc(data.request.product)}</p></div><button class="button secondary" onclick="go('detail/${encodeURIComponent(id)}')">Back to request</button></div><div class="hero-card"><span class="eyebrow" style="color:var(--yellow)">Recommended</span><h2>${esc(rec.vendor_name)} <span class="score">${rec.final_score}</span></h2><p>${esc(data.explanation)}</p><div class="actions"><button class="button" onclick="approve('${esc(id)}')">Approve & create PO</button><button class="button danger" onclick="reject('${esc(id)}')">Reject request</button></div></div><section class="vendor-grid">${data.vendors.map(v=>`<article class="vendor"><h4>${esc(v.vendor_name)}</h4><span class="badge ${v===rec?'done':''}">${v===rec?'Top choice':'Alternative'}</span><span class="score">${v.final_score}<small class="muted"> / 100</small></span><div class="muted">${money(v.raw_price)} / unit · ${v.raw_delivery} days</div><p><span class="badge ${v.risk_level==='LOW'?'done':'warn'}">${v.risk_level} risk</span></p></article>`).join('')}</section>`}
async function approvals(){const items=await api('/approvals');return `<div class="topbar"><div><span class="eyebrow">Decision queue</span><h1>Approvals</h1><p>Review recommendations and move confident requests into purchasing.</p></div></div><section class="panel">${items.length?items.map(item=>`<div style="display:flex;justify-content:space-between;align-items:center;gap:20px;padding:18px 0;border-bottom:1px solid var(--line)"><div><a class="link" onclick="go('detail/${encodeURIComponent(item.request.id)}')">${esc(item.request.id)}</a><h3 style="margin:5px 0">${esc(item.request.product)}</h3><span class="muted">${esc(item.recommended.vendor_name)} · ${money(item.recommended.raw_price*item.request.quantity)} total</span></div><div class="actions"><span class="badge warn">${esc(item.required_approval.replaceAll('_',' '))}</span><button class="button" onclick="approve('${esc(item.request.id)}')">Approve</button><button class="button danger" onclick="reject('${esc(item.request.id)}')">Reject</button></div></div>`).join(''):'<div class="empty">Everything is up to date.</div>'}</section>`}
async function vendors(){const items=await api('/vendors/performance');return `<div class="topbar"><div><span class="eyebrow">Supplier intelligence</span><h1>Vendor performance</h1><p>Compare reliability, accuracy, delivery, and commercial fit.</p></div></div><section class="vendor-grid">${items.map(v=>`<article class="vendor"><span class="eyebrow">${esc(v.category||'Supplier')}</span><h4>${esc(v.name)}</h4><span class="score">${v.overall}<small class="muted"> / 100 overall</small></span><div class="muted">Reliability ${v.reliability}% · Accuracy ${v.accuracy}%</div><div class="muted" style="margin-top:6px">Delivery ${v.delivery}% · Rating ${v.rating}</div></article>`).join('')}</section>`}
async function render(view){const main=document.getElementById('main');main.innerHTML='<div class="loading-screen" style="min-height:40vh"><span class="pulse"></span><span>Updating workspace</span></div>';try{let html=view==='dashboard'?await dashboard():view==='requests'?await requests():view==='new'?form():view==='approvals'?await approvals():view==='vendors'?await vendors():view.startsWith('detail/')?await detail(decodeURIComponent(view.slice(7))):await compare(decodeURIComponent(view.slice(8)));main.innerHTML=html;if(view==='new')document.getElementById('request-form').onsubmit=createRequest}catch(error){main.innerHTML=`<div class="notice">${esc(error.message)}</div>`}}
async function createRequest(event){event.preventDefault();try{const data=Object.fromEntries(new FormData(event.target));const pr=await api('/purchase-requests',{method:'POST',body:JSON.stringify(data)});toast(`${pr.id} created`);go(`detail/${encodeURIComponent(pr.id)}`)}catch(error){toast(error.message)}}
async function analyze(id){try{await api(`/purchase-requests/${id}/analyze`,{method:'POST'});toast('Vendor analysis complete');go(`compare/${encodeURIComponent(id)}`)}catch(error){toast(error.message)}}
async function approve(id){if(!confirm('Approve this request and create a purchase order?'))return;try{const result=await api(`/purchase-requests/${id}/approve`,{method:'POST'});toast(`PO ${result.po_id} created`);go(`detail/${encodeURIComponent(id)}`)}catch(error){toast(error.message)}}
async function reject(id){const reason=prompt('Reason for rejection:');if(!reason?.trim())return;try{await api(`/purchase-requests/${id}/reject`,{method:'POST',body:JSON.stringify({reason})});toast('Request rejected');go(`detail/${encodeURIComponent(id)}`)}catch(error){toast(error.message)}}
async function logout(){await api('/auth/logout',{method:'POST'});location.hash='';boot()}
function go(view){location.hash=`#${view}`;boot()}
async function boot(){try{const auth=await api('/auth/me');const path=location.pathname;if(!auth.authenticated){if(path==='/'||path==='/index.html'){root.innerHTML=landing(false);observeReveals()}else{await login()}return}if(path==='/'||path==='/index.html'){root.innerHTML=landing(true);observeReveals();return}const view=location.hash.slice(1)||'dashboard';root.innerHTML=shell(auth.user,view.split('/')[0]);await render(view)}catch(error){root.innerHTML=`<div class="empty">${esc(error.message)}</div>`}}
function observeReveals(){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')}),{threshold:.15});document.querySelectorAll('.reveal').forEach(item=>observer.observe(item))}
boot();

async function orders(){const items=await api('/purchase-orders');return `<div class="topbar"><div><span class="eyebrow">ERP purchasing</span><h1>Purchase orders</h1><p>Orders created from approved procurement decisions.</p></div></div><section class="panel">${items.length?`<table class="table"><thead><tr><th>Order</th><th>Vendor</th><th>Total</th><th>Status</th></tr></thead><tbody>${items.map(order=>`<tr><td><strong>${esc(order.name||order.id)}</strong></td><td>${esc(order.partner_id?.[1]||'Vendor')}</td><td>${money(order.amount_total)}</td><td><span class="badge done">${esc(order.state||'purchase')}</span></td></tr>`).join('')}</tbody></table>`:'<div class="empty">No purchase orders are available for your role.</div>'}</section>`}
async function analytics(){const data=await api('/analytics');return `<div class="topbar"><div><span class="eyebrow">Procurement intelligence</span><h1>Analytics</h1><p>Signals from the authorized procurement workspace.</p></div></div><div class="stats">${[['Spend',money(data.spend)],['Purchase orders',data.purchase_order_count],['Requests',data.request_count],['Vendors',data.vendor_count]].map(item=>`<div class="stat"><span class="panel-label">${item[0]}</span><strong>${item[1]}</strong></div>`).join('')}</div><section class="panel"><h3>Risk distribution</h3><p class="muted">${data.risk_distribution.low} vendors currently sit in the low-risk band. ${data.risk_distribution.attention} need attention.</p></section>`}
async function render(view){const main=document.getElementById('main');main.innerHTML='<div class="loading-screen" style="min-height:40vh"><span class="pulse"></span><span>Updating workspace</span></div>';try{let html=view==='dashboard'?await dashboard():view==='requests'?await requests():view==='new'?form():view==='approvals'?await approvals():view==='vendors'?await vendors():view==='orders'?await orders():view==='analytics'?await analytics():view.startsWith('detail/')?await detail(decodeURIComponent(view.slice(7))):await compare(decodeURIComponent(view.slice(8)));main.innerHTML=html;if(view==='new')document.getElementById('request-form').onsubmit=createRequest}catch(error){main.innerHTML=`<div class="notice">${esc(error.message)}</div>`}}

async function vendors(){return `<div class="topbar"><div><span class="eyebrow">Supplier intelligence</span><h1>Find vendors</h1><p>Discover nearby suppliers for a product, service, and location.</p></div></div><section class="panel"><form id="vendor-search-form" onsubmit="searchVendors(event)"><div class="form-grid"><div class="field"><label>What do you need?</label><input name="query" required placeholder="e.g. bulk office chairs"></div><div class="field"><label>Delivery location</label><input name="location" required placeholder="e.g. Pune"></div><div class="field"><label>Search radius (metres)</label><input name="radius" type="number" min="1" placeholder="Optional"></div></div><button class="button">Search vendors</button></form><div id="vendor-results" style="margin-top:26px"></div></section>`}
async function searchVendors(event){event.preventDefault();const form=event.target;const results=document.getElementById('vendor-results');results.innerHTML='<div class="empty">Searching authorized location provider...</div>';const params=new URLSearchParams(new FormData(form));try{const data=await api(`/vendors/search?${params}`);if(!data.results.length){results.innerHTML='<div class="empty">No vendors found for this search.</div>';return}results.innerHTML=`<div class="vendor-grid">${data.results.map(v=>`<article class="vendor"><h4>${esc(v.name||'Unnamed vendor')}</h4><div class="muted">${esc(v.formatted_address||v.vicinity||'Location provided by Google')}</div>${v.rating?`<span class="score">${v.rating}<small class="muted"> rating</small></span>`:''}</article>`).join('')}</div>`}catch(error){results.innerHTML=`<div class="notice">${esc(error.message)}</div>`}}
